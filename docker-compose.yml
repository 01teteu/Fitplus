services:
  db:
    image: postgres:15
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: fit_plus
    ports:
      - "5432:5432"
    volumes:
      - fit_plus_data:/var/lib/postgresql/data

volumes:
  fit_plus_data: